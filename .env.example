# PostgreSQL in the main yanbridge compose network.
# If this server joins yanbridge_default, "db:5432" should resolve to the main Postgres service.
DATABASE_URL=postgresql://yanbridge:yanbridge@db:5432/yanbridge?sslmode=disable

# Required when pipeline is enabled.
DEEPSEEK_API_KEY=
